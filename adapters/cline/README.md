# cline adapter

**status:** planned.

cline exposes `rules` and `workflows`. the aegis hooks can map as:

- rules → wrap L1 patterns into a "detect injection" rule
- workflows → run `integrity-check.sh verify` as a scheduled workflow

planned for v1.1. contributions welcome.
